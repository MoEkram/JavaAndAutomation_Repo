package i_Interfaces;

// Interface = Same as class, its collection of abstract methods. ( only structure without code )
//				A class is "implements" an interface << inherit the abstract methods of the interface
//	error will be displayed in case adding new method to interface and not added to implementation


// Create interface

public interface a_AnimalInterface {

	public void eat();
	public void travel();
	
}
